import {Routes} from '@angular/router';
import {RegisterComponent} from './register.component';

export const RegisterManagementRoutes: Routes = [
  {
    path: 'register',
    component: RegisterComponent
  }];
